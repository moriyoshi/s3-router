apiVersion: v1
kind: Service
metadata:
  name: {{ include "s3-router.fullname" . }}
  labels:
    {{- include "s3-router.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.adminPort }}
      targetPort: admin
      protocol: TCP
      name: admin
  selector:
    {{- include "s3-router.selectorLabels" . | nindent 4 }}
